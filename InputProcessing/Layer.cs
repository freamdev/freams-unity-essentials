using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Enum used instead of integers, add your custom layers to the list
/// </summary>
public enum Layer {
    Ui = 5,
    Ground = 8,
    RaycastEndStop = -1
}
